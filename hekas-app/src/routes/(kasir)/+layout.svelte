<script lang="ts">
	/**
	 * Kasir route group layout (placeholder)
	 *
	 * Akan berisi:
	 * - Sidebar POS
	 * - TopBar kasir
	 * - RBAC guard (cek role = 'kasir')
	 *
	 * Untuk sekarang pass-through dulu sampai R0.1b/0.4 selesai.
	 */
	let { children } = $props();
</script>

{@render children()}
