# ─── HEKAS POS — Frontend env ─────────────────────────────────────────
# Copy file ini ke .env dan sesuaikan dengan environment Anda.

# Backend API base URL (Wafiq ElysiaJS).
#   - DEV (BE Wafiq di laptop dia): http://localhost:3001/api
#   - PROD (VPS): https://api.hekas.id/api
#   - KOSONGKAN untuk pakai localStorage mock (FE-only standalone mode).
# VITE_API_BASE=http://localhost:3001/api
